#include <iostream>
// using namespace std;  // SEARCH command
// cout, endl, int, string, etc. lives in std, which is under the iostream library
// namespace  // we can make custom namespace of our own

int main() {
    // std is short for: "standard" C++ namespace
    // not actually standard across all compilers
    // "junk drawer"
    std::cout << "hello world" << std::endl;
    std::string s = "hi there";
}
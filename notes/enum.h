// declare datatypes
// inside a .h file
// declaration only, no code
#ifndef ENUM_H
#define ENUM_H

enum Suit {HEART, CLUB, DIAMOND, SPADE};

enum DaysOfTheWeek {
    MONDAY,
    TUESDAY,
    WEDNESDAY
};

#endif /* ENUM_H */
// declare datatypes
// inside a .h file
// declaration only, no code
#ifndef enum
#define enum.h

enum Suit {HEART, CLUB, DIAMOND, SPADE};

enum DaysOfTheWeek {
    MONDAY,
    TUESDAY,
    WEDNESDAY
};

#endif